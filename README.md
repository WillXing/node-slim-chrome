## Docker NodeJS slim with Chrome

This is the docker image base on NodeJS with chrome installed.

### Usage
In the Dockerfile, just use following to get the base

```
FROM dockerxw/node-slim-chrome:latest
```
